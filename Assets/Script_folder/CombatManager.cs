using UnityEngine;

/// <summary>
/// Empty placeholder - just the default Start()/Update() template, never filled in. Not
/// attached to anything and not referenced by any other script. Either implement it or
/// remove it; leaving it as-is risks someone assuming it does something it doesn't.
///
/// Setup: none currently - nothing to configure until this has actual behavior.
/// </summary>
public class CombatManager : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
