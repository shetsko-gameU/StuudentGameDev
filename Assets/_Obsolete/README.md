# Obsolete leftovers

Bob placeholder `.anim` clips and the legacy `Player/Prefabs/Player.prefab`.

**Do not** assign these onto playable Animator Controllers. Source of truth is baked takes in `Assets/Fbx_exports/`.
Re-run **Tools → Animations → Wire Real FBX Clips** after new exports.
